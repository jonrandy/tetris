export default function({
	skin = 'default',
	document,
	debugMode = false
} = {}) {

	function update({
		score,
		state,
		board,
		piece,
		nextPieces,
		level
	}) {

		

	}

	return Object.freeze({
		update
	});

}